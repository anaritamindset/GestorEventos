# Example environment variables for Google App Engine
# Copy this file to .env.yaml and fill in your values
# DO NOT commit .env.yaml to git!

env_variables:
  # Email configuration for Ana Rita
  EMAIL_PASSWORD: "your-gmail-app-password-here"

  # Email configuration for ARdaTerra
  ARDATERRA_EMAIL: "ardoterra@gmail.com"
  ARDATERRA_PASSWORD: "your-ardaterra-app-password-here"

  # Flask secret key (generate a random string)
  SECRET_KEY: "your-secret-key-here-make-it-long-and-random"

  # Flask environment
  FLASK_ENV: "production"

  # Optional: Database URL (if using Cloud SQL)
  # DATABASE_URL: "postgresql://user:password@/dbname?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME"
